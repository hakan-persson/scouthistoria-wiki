mediawikiUser: wikiadmin
mediawikiName: Scouthistoria
mediawikiHost: scouthistoria.pertech.se
mediawikiEmail: scouthistoria@scouterna.se
mediawikiPassword: "cpfPFnyEWg"   # Empty at first installation. Must contain a value when upgrading.
allowEmptyPassword: no

image:
  registry: ghcr.io
  repository: hakan-persson/scouthistoria-wiki
  tag: main
  # registry: docker.io
  # repository: bitnami/mediawiki
  # tag: 1.41.0-debian-11-r0
  # pullPolicy: IfNotPresent
  pullPolicy: Always

mariadb:
  auth:
    rootPassword: "ojR4gkTIOK"   # Empty at first installation. Must contain a value when upgrading.
    password: "EZrp4wuXRb"   # Empty at first installation. Must contain a value when upgrading.

service:
  type: ClusterIP

ingress:
  # ingressClassName: nginx
  enabled: true
  hostname: scouthistoria.pertech.se
  tls: true
  pathType: Prefix
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt
    nginx.ingress.kubernetes.io/backend-protocol: http

livenessProbe:
  enabled: false
readinessProbe:
  enabled: false
